#ifndef H_RAYMOND_CONSTANTS
#define H_RAYMOND_CONSTANTS

const double EPSILON = 0.0001;

const int RAY_DEPTH_LIMIT = 5;

const double SAFE_DIV_MIN = EPSILON;
const double SAFE_DIV_MAX = (1.0f / SAFE_DIV_MIN);

#endif
